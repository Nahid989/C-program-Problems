// #include <graphics.h> // Non-standard header
#include <stdio.h>

/*
int main() {
    int gd = DETECT, gm;
    int x = 250, y = 250; // Center coordinates
    int radius = 100;

    // Initialize the graphics system
    initgraph(&gd, &gm, "C:\\TURBOC3\\BGI");

    // Draw a circle
    circle(x, y, radius);

    getch(); // Wait for a key press
    closegraph(); // Close the graphics mode
    return 0;
}
*/
int main() {
    printf("The C Program to Draw a Circle requires the non-standard <graphics.h> library.\n");
    printf("The conceptual code (if <graphics.h> were available) is commented out above.\n");
    return 0;
}
